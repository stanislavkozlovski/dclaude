# Lessons

- Keep `README.md`, `ARCHITECTURE.md`, and the wrapper/runtime scripts aligned. This repo is mostly behavior encoded in docs and shell, so drift is the main failure mode.
- If the mount model or persisted state changes, update `ARCHITECTURE.md` in the same change.
